# Guidelines for developers

In this project the submodules use HTTPS type URLS. In case you prefer contributions with SSH keys, enable it e.g. with

```
git config --global url."git@github.com:org/".insteadOf "https://github.com/org/"
```
